


axis <- function(position, 
                 title = NULL, 
                 categoryFields = NULL, 
                 measure = NULL, 
                 timeField = NULL) {
  list(position = position,
       title = title,
       categoryFields = categoryFields,
       measure = measure,
       timeField = timeField)
}

